# helm diff upgrade --install -n localhost with-only-job ./charts/base/ -f ./examples/base/with-only-job.yaml

workloadType: disabled-to-have-only-job

service:
  enabled: false

job:
  name: db-migration-backend
  image:
    repository: repo
    tag: latest
    pullPolicy: Always
  envFrom:
    secret: secret
    configMap: configMap
  command:
    - "sh"
    - "-c"
    - |
      set -e
