
kind: Job
service:
  enabled: false

job:
  name: db-migration-backend
  image:
    repository: repo
    tag: latest
    pullPolicy: Always
  envFrom:
    secret: secret
    configMap: configMap
  command:
    - "sh"
    - "-c"
    - |
      set -e
