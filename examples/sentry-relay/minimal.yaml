# helm diff upgrade --install -n localhost sentry-relay-minimal ./charts/sentry-relay/ -f ./examples/sentry-relay/minimal.yaml
# Minimal example (proxy mode). Replace upstream with your Sentry org ingest URL. See charts/sentry-relay/values.yaml.
upstream: https://YOUR-ORGANIZATION-ID.ingest.sentry.io
