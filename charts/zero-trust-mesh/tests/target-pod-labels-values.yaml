workload: frontend
namespaceResourcesEnabled: false
allowTo:
  - service: backend
    targetPodLabels:
      app: backend
      component: api
    port: 8080
    methods: ["GET"]
    paths: ["/api/*"]
