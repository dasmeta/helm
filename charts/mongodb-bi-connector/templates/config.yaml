apiVersion: v1
kind: ConfigMap
metadata:
  name: mongosqld-config
data:
  config.yaml: |
    {{- $mergedConfig := mergeOverwrite .Values.mongodbBiConnector.mongosqldConfigDefault .Values.mongodbBiConnector.mongosqldConfig }}
{{ $mergedConfig | toYaml | indent 4 }}
