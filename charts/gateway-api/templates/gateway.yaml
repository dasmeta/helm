{{- if .Values.gateways }}
{{- $outer := . -}}
{{- $gateways := .Values.gateways -}}
{{- if $gateways }}
{{- range $idx, $gateway := (ternary $gateways (list $gateways) (kindIs "slice" $gateways)) }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ default (include "gateway-api.fullname" $outer) $gateway.name }}{{ coalesce $gateway.nameSuffix "" }}
  namespace: {{ default "istio-system" (default $gateway.namespace $outer.Release.Namespace) | quote }}
  labels:
    {{- include "gateway-api.labels" $outer | nindent 4 }}
    {{- with $gateway.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with $gateway.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  gatewayClassName: {{ default "istio" $gateway.gatewayClassName | quote }}
  {{- with $gateway.addresses }}
  addresses:
    {{- range . }}
    - {{- toYaml . | nindent 6 }}
    {{- end }}
  {{- end }}
  {{- with $gateway.listeners }}
  listeners:
    {{- range . }}
    {{- $protocol := default "HTTP" .protocol }}
    {{- $port := default 80 .port }}
    {{- $hostname := .hostname | default "" }}
    {{- if .name }}
    - name: {{ .name | quote }}
    {{- else }}
    {{- $hostnameSanitized := $hostname | replace "." "-" | replace "*" "wildcard" }}
    {{- if not $hostnameSanitized }}
    {{- $hostnameSanitized = "default" }}
    {{- end }}
    {{- $protocolLower := $protocol | lower }}
    {{- $generatedName := printf "%s-%d-%s" $protocolLower $port $hostnameSanitized }}
    {{- $nameLength := len $generatedName }}
    {{- if gt $nameLength 63 }}
    {{- $generatedName = slice $generatedName 0 63 }}
    {{- end }}
    - name: {{ $generatedName | quote }}
    {{- end }}
      {{- if $hostname }}
      hostname: {{ $hostname | quote }}
      {{- end }}
      port: {{ $port }}
      protocol: {{ $protocol | quote }}
      {{- with .tls }}
      tls:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .allowedRoutes }}
      allowedRoutes:
        {{- toYaml .allowedRoutes | nindent 8 }}
      {{- else }}
      allowedRoutes:
        namespaces:
          from: "All"
      {{- end }}
    {{- end }}
  {{- end }}
  {{- with $gateway.infrastructure }}
  infrastructure:
    {{- with .annotations }}
    annotations:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- with .parametersRef }}
    parametersRef:
      {{- if .group }}
      group: {{ .group | quote }}
      {{- end }}
      {{- if .kind }}
      kind: {{ .kind | quote }}
      {{- end }}
      {{- if .name }}
      name: {{ .name | quote }}
      {{- end }}
      {{- if .namespace }}
      namespace: {{ .namespace | quote }}
      {{- end }}
    {{- end }}
  {{- end }}
{{- end }}
{{- end }}
{{- end }}
